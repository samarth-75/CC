export function battleSocketHandler(io, socket) {
  // Your socket event logic here
  // Example:
  socket.on('joinArena', (data) => {
    // handle joinArena event
  });

  // Add other socket events...
}